# ============================================================================
# APP CONFIGURATION - TEST
# ============================================================================
PUBLIC_NAME_APP=test_app_e2e
NODE_ENV=TEST
PUBLIC_ENV=TEST
PORT=3005
PUBLIC_URL=http://localhost:3005
PUBLIC_PORT=3005

# ============================================================================
# DATABASE (Real PostgreSQL - Test dedicated)
# ============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=test_db
DB_USER=postgres
DB_PASS=dokixd123
DATABASE_URL=postgresql://postgres:dokixd123@localhost:5432/test_db?schema=public

# ============================================================================
# CACHE (Mocked in tests usually, but provided for full config)
# ============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================================================
# JWT - SEGURIDAD (Faster expiration for tests)
# ============================================================================
JWT_KEY=test-jwt-secret-key-minimum-32-characters-long
JWT_EXPIRES_IN=1h
JWT_REFRESH_KEY=test-refresh-secret-key-minimum-32-characters-long
JWT_REFRESH_EXPIRES_IN=24h

# ============================================================================
# SESSION (For Auth tests)
# ============================================================================
SESSION_SECRET=test-session-secret-key

# ============================================================================
# HMAC
# ============================================================================
PUBLIC_HMAC_KEY=test-hmac-key-minimum-16-characters-long

# ============================================================================
# STORAGE (Mocked or Temp)
# ============================================================================
STORAGE_PROVIDER=LOCAL
RUSTFS_ENDPOINT=localhost
RUSTFS_PORT=9000
RUSTFS_ROOT_USER=admin
RUSTFS_ROOT_PASSWORD=admin123
RUSTFS_BUCKET_NAME=test-bucket
RUSTFS_REGION=us-east-1

# ============================================================================
# MAIL (Mocked)
# ============================================================================
MAIL_HOST=smtp.ethereal.email
MAIL_PORT=587
MAIL_USER=test_user
MAIL_PASS=test_pass
MAIL_FROM=test@example.com

# ============================================================================
# EXTRAS
# ============================================================================
CORS_ORIGINS=*
THROTTLE_TTL=60
THROTTLE_LIMIT=1000
LOG_LEVEL=ERROR
OPENROUTER_API_KEY=test-openrouter-key
OPENAI_API_KEY=test-openai-key
